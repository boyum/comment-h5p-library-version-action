name: Test

on:
  - pull_request
  - workflow_dispatch

jobs:
  test:
    name: Test

    runs-on: ubuntu-latest

    steps:
      - name: Test
        uses: ./
        with:
          working-directory: demo

